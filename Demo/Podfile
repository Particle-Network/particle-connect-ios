
platform :ios, '14.0'

target 'Demo' do
source 'https://github.com/CocoaPods/Specs.git'
  use_frameworks!

  pod 'ConnectWalletConnectAdapter', '0.2.6'
  pod 'ConnectEVMAdapter', '0.2.6'
  pod 'ConnectSolanaAdapter', '0.2.6'
  pod 'ConnectPhantomAdapter', '0.2.6'
  pod 'ParticleConnect', '0.2.6'
  pod 'ConnectCommon', '0.2.6'
  pod 'ParticleAuthAdapter', '0.2.6'
  
  pod 'ParticleWalletAPI', '1.0.0'
  pod 'ParticleNetworkBase', '1.0.0'
  pod 'ParticleAuthService', '1.0.0'
  pod 'SDWebImage'
  pod 'SnapKit'
  

end


post_install do |installer|
  
    installer.pods_project.targets.each do |target|
      target.build_configurations.each do |config|
      config.build_settings['BUILD_LIBRARY_FOR_DISTRIBUTION'] = 'YES'
        end
      end
    
    installer.pods_project.build_configurations.each do |config|
      config.build_settings["EXCLUDED_ARCHS[sdk=iphonesimulator*]"] = "x86_64"
      config.build_settings["ARCHS[sdk=iphonesimulator*]"] = "arm64"
      config.build_settings["ARCHS[sdk=iphoneos*]"] = "arm64"
    end

  # This is necessary for Xcode 14, because it signs resource bundles by default
  # when building for devices.
  installer.target_installation_results.pod_target_installation_results
  .each do |pod_name, target_installation_result|
    target_installation_result.resource_bundle_targets.each do |resource_bundle_target|
      resource_bundle_target.build_configurations.each do |config|
        config.build_settings['CODE_SIGNING_ALLOWED'] = 'NO'
      end
    end
  end
end
